#!/usr/bin/env bash
set -e

main() {
  export TESTING=1

  ARGS="${@}"

  if [[ "${ARGS}" == *'--watch'* ]]; then
    ARGS="${ARGS/'--watch'/}"
    ./.venv/bin/ptw -- $ARGS
  else
    ./.venv/bin/pytest $ARGS
  fi
}

main "$@"
