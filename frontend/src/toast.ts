// eslint-disable-next-line no-restricted-imports
import toast, { Toaster } from "react-hot-toast"

// Things to improve:
// - sticky toasts
// - dismissable toasts
// - more typescript friendly api, like toast({ message, type: "success" })
export { toast, Toaster }
