# every 5 minutes
# also get logs script to docker output
# from: https://stackoverflow.com/a/46220104/3720597
SHELL=/bin/bash
BASH_ENV=/var/container.env
# Note: we copy over the pgdump cron entry at container run time so that the
# cron interval can be configured via the env.
